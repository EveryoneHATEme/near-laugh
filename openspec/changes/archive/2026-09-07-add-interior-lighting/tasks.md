## 1. Establish the T1 measurement baseline

- [x] 1.1 Record the selected GPU device/vendor IDs, CPU, RAM, driver, power mode, 1920x1080 framebuffer and 60 Hz presentation setup in `validation.md`; verify they identify the actual measurement machine and distinguish planned targets from observations.
- [x] 1.2 Add bounded opt-in development timing for CPU active work, separate fence/acquire/present waits, GPU whole frame and later shadow work; verify timestamp availability, period/valid-bit conversion and fence-safe readback, with explicit unavailable reporting rather than inferred GPU time.
- [x] 1.3 Configure a separate Clang/Ninja release build (for example `cmake -S . -B build/p10-release -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++`), build it and collect the current furnished unshadowed baseline using the design's warm-up/sample policy; retain commands, scene/view and raw timing results in the validation record before substantial performance changes.

## 2. Authored lighting and v8 persistence

- [x] 2.1 Replace fixed point-light/single-switch definitions with the bounded ID-bearing light/switch collections and per-light initial/shadow fields; verify zero/eight lights, zero/sixteen switches, ambient endpoints, ID uniqueness, references, numeric safety and the four-shadow-caster budget including disabled sources through shared-validation tests.
- [x] 2.2 Extend the strict codec to v8 and preserve exact v2-v7 read shapes with the documented deterministic light/switch mapping; verify legacy null/initially-off switches, slot-to-ID mapping, rejection of mixed shapes and unchanged geometry/material/entry/door/audio data using saved fixtures including a retained v7 fixture.
- [x] 2.3 Implement canonical v8 saving and clean read-only migration notices; verify byte-identical save/load/save, collection ordering, light identity after reordering, no write on open, Save As behavior and preservation of the last usable file/document after failed load/save.
- [x] 2.4 Update the immutable level handoff, starter Interior, packaged levels and their preparation scripts for v8; verify startup/default entries, deterministic regeneration and mapped P01/P04 light appearance/enables while retaining legacy test files in their original versions.

## 3. Runtime light ownership and interaction

- [x] 3.1 Extend the concrete light controller to initialize and own one run-local enable per authored light and resolve each switch to one light; verify unlinked lights, two switches sharing a light, repeated toggles, independent ambient/flashlight values, immutable definitions and fresh-run reset.
- [x] 3.2 Extend targeting to all switch plates with nearest-distance/tolerance/type/ID ordering and the existing obstruction policy; verify collection-order independence, door/switch ties, inside/out-of-reach queries, blocked and unsupported nearest actions, and no fallthrough.
- [x] 3.3 Preserve sampled release latches and R/E/knock priority across zero/multiple fixed steps, missed presses, cursor release/recapture and minimized waits; verify each eligible edge acts at most once and no inactive or held press becomes delayed activation.
- [x] 3.4 Replace the two frame enables with exact-size backend-neutral state matching the loaded light set, and update callers; verify empty sets and mismatched input handling before submission, plus state retention across skipped/recovered frames without exposing switch/controller/backend types.

## 4. Bounded Vulkan lighting and shadows

- [x] 4.1 Update scene light uploads and shading for the bounded set and authored ambient, keeping material behavior and the existing push-constant ceiling; verify independent enables with/without the spotlight, zero lights, ambient zero, invalid boundary data and all-eight-light rendering.
- [x] 4.2 Implement renderer-owned frame-slot shadow images/views, valid dummy resources, descriptors and depth-format/profile checks with RAII cleanup; verify supported-feature selection, byte/count overflow rejection, injected partial-construction failures and resources surviving presentation recovery.
- [x] 4.3 Implement six depth views per enabled shadowed point light from static material batches and the same accepted changing boxes as the color pass; verify camera-independent blockers, door/geometry agreement and independent OPAQUE/MASK coverage, including a prop with no collision proxies.
- [x] 4.4 Implement point-light face projection, depth comparison, bounded filtering and cross-face sampling with bias; verify face selection/depth math with numeric behavior tests and fixed-view GPU readbacks for lit, wall-blocked, door-closed/open and face-boundary cases using the specified patch tolerances.
- [x] 4.5 Integrate shadow writes, scene reads and subsequent reuse on the graphics queue with explicit Synchronization 2 dependencies; verify fenced uploads, current changing geometry, all light combinations, zero extent, resize/minimize/recovery and teardown through Vulkan validation.
- [x] 4.6 Package new shadow shaders alongside the scene stages, regenerate SPIR-V and run `spirv-val` for every changed stage under the Vulkan 1.3 profile; retain the existing SPIR-V 1.5 discard compatibility where needed and verify missing-shader diagnostics and cleanup through the existing resource tests/smoke.

## 5. Light and switch authoring

- [x] 5.1 Add per-object transient selection and bounded light/switch add, duplicate, delete and property commands; verify fresh durable IDs, default values, outgoing/incoming link rules, count-limit refusal, repairable shadow-budget invalidity and one-step undo/redo with the same IDs and dirty-state transitions.
- [x] 5.2 Make light rename update every incoming switch link atomically, validate switch rename and retain broken links after deletion; verify shared-link rename/undo, delete/repair/undo and lightless switch creation without cascading or silent retargeting.
- [x] 5.3 Expose light ID/parameters/initial/shadow flags, ambient, switch ID/link and budget diagnostics; extend list/viewport selection, range/link overlays and surface placement while preserving capture/cancel rules. Verify with actual UI command/input tests and saved-document round trips.
- [x] 5.4 Share the runtime lighting/shadow implementation with editor initial-state preview without gameplay physics; verify matching initial light/door captures, relink/remove without changing light initial values, zero-light scenes and no state leaks from a separate playtest process.
- [x] 5.5 Make geometry/light/shadow preview replacement transactional and safe for invalid documents; verify failed allocation retains an explicitly stale coherent preview, correction/undo recovers it, terrain edits update shadow occluders and resize/minimize/document replacement retain correct values.
- [x] 5.6 Integrate light/reference validation with Save/Play preflight and legacy migration notices; verify invalid links or profile failures launch no game, dirty Save and Play uses the saved v8 snapshot, and a Unicode path from another working directory remains valid.

## 6. T1 acceptance and completion evidence

- [x] 6.1 Author/package the separate neutral six-light T1 interior and eight-light capacity setup with four shadowed sources, at least six switches, a shared link, two doors and inspection starts; verify full validation, ordinary walking/door reachability, fresh startup and editor-to-saved-file Play without filename-driven behavior.
- [x] 6.2 Extend deterministic and Vulkan smoke integration coverage for migrated P04 audio, switching through door obstruction, changing shadows, material cutouts, editor edits/history, failed replacement, presentation recovery and final destruction; verify the existing suites and new observable behavior checks without source-text assertions on incidental implementation shape.
- [x] 6.3 Follow `docs/DEVELOPMENT.md`: run `cmake --preset debug`, `cmake --build --preset debug`, `ctest --preset debug --output-on-failure`, changed-shader validation and `ctest --preset vulkan-smoke --output-on-failure`; retain results and explicitly record unavailable checks in `validation.md`.
- [x] 6.4 Perform and record T1 visual acceptance: isolated wall/closed-door patch comparisons, opened/player-obstructed/reversed doors, off-screen blockers, face seams, opaque and MASK furniture, flashlight independence, dark-route readability and editor/game initial agreement; retain captures/coordinates/tolerances and identify any unacceptable leaks or limitations.
- [x] 6.5 Build the final release target and repeat three warmed-up 60-second runs for both furnished and capacity setups using the design's exact conditions; record CPU/GPU/frame percentiles, separated waits, shadow cost, memory use, equivalent unshadowed baselines and results against every gate. If a gate fails, investigate measured causes and revise the design explicitly before adding substantial complexity or claiming T1 acceptance.
- [x] 6.6 Update current architecture, rendering, gameplay and development documentation for v8, light ownership, supported shadow limits and reproducible T1 checks; review `git diff` and run `openspec validate add-interior-lighting --strict`. Keep roadmap status and `validation.md` consistent with actual completed evidence and flag only unresolved acceptance rather than claiming later P07/P05/P09 integration.
